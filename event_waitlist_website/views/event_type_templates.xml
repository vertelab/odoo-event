<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Index -->
    <template id="index_event_types" name="Event Types" track="1">
        <t t-call="website.layout">
            <t t-set="head">
                <meta t-if="search_tags" name="robots" content="none"/>
            </t>

            <div id="wrap" class="o_wevent_index">
                <!-- Options -->
                <t t-set="opt_events_list_cards" t-value="is_view_active('event_waitlist_website.opt_events_list_cards')"/>
                <t t-set="opt_events_list_columns" t-value="is_view_active('event_waitlist_website.opt_events_list_columns')"/>
                <!-- Topbar -->
                <t t-call="event_waitlist_website.index_topbar">
                    <t t-set="search" t-value="original_search or search or searches['search']"/>
                </t>
                <!-- Drag/Drop Area -->
                <div id="oe_structure_we_index_1" class="oe_structure oe_empty"/>
                <!-- Content -->
                <div class="o_wevent_events_list">
                    <div class="container">
                        <t t-call="event_waitlist_website.searched_tags"/>
                        <div class="row">
                            <div id="o_wevent_index_main_col" t-attf-class="col-md mb-3 #{opt_events_list_columns and 'opt_events_list_columns' or 'opt_events_list_rows'}">
                                <div class="row g-4 g-lg-3 g-xxl-4">
                                    <!-- Events List -->
                                    <t t-call="event_waitlist_website.events_list"/>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Drag/Drop Area -->
                <div id="oe_structure_we_index_2" class="oe_structure oe_empty"/>
            </div>
        </t>
    </template>

    <!-- Index - OPTION - Sidebar -->
    <template id="opt_index_sidebar" inherit_id="event_waitlist_website.index_event_types" active="False" name="Show Sidebar">
        <xpath expr="//div[@id='o_wevent_index_main_col']" position="after">
            <t t-call="event_waitlist_website.index_sidebar"/>
        </xpath>
    </template>

    <!-- Index Topbar -->
    <template id="index_topbar" name="Topbar">
        <div class="container mt-3 mb-4">
            <div class="o_wevent_index_topbar_filters d-flex d-print-none align-items-center justify-content-end flex-wrap gap-2 w-100">
                <h2 class="h4 my-0 me-auto pe-sm-4">Event Types</h2>
                    <t t-foreach="categories" t-as="category">
                        <div t-if="category.is_published and category.tag_ids and any(tag.color for tag in category.tag_ids)" class="dropdown d-none d-lg-block">
                            <a href="#" role="button" class="btn btn-light dropdown-toggle" data-bs-toggle="dropdown">
                                <t t-out="category.name"/>
                            </a>
                            <div class="dropdown-menu">
                                <t t-foreach="category.tag_ids" t-as="tag">
                                    <a t-if="tag.color"
                                        t-att-href="'/event-types?%s' % keep_query('*', tags=str((search_tags - tag).ids if tag in search_tags else (tag | search_tags).ids))"
                                        t-attf-class="dropdown-item d-flex align-items-center justify-content-between #{'active' if tag in search_tags else ''}">
                                        <t t-out="tag.name"/>
                                    </a>
                                </t>
                            </div>
                        </div>
                    </t>
                <div class="o_wevent_search d-flex w-100 w-lg-auto">
                    <div class="w-100 flex-grow-1">
                        <t t-call="event_waitlist_website.event_types_search_box_input"/>
                    </div>
                    <button class="btn btn-light position-relative ms-2 d-lg-none"
                        data-bs-toggle="offcanvas"
                        data-bs-target="#o_wevent_index_offcanvas">
                        <i class="fa fa-sliders"/>
                    </button>
                </div>
            </div>
            <!-- Off canvas filters on mobile-->
            <div id="o_wevent_index_offcanvas" class="o_website_offcanvas offcanvas offcanvas-end d-lg-none p-0 overflow-visible mw-75">
                <div class="offcanvas-header">
                    <h5 class="offcanvas-title">Filters</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"/>
                </div>
                <div class="offcanvas-body p-0">
                    <div class="accordion accordion-flush">
                        <t t-foreach="categories" t-as="category">
                            <div class="accordion-item">
                                <h2 class="accordion-header">
                                    <button class="accordion-button collapsed"
                                        type="button"
                                        data-bs-toggle="collapse"
                                        t-attf-aria-controls="o_wevent_offcanvas_cat_#{category.name}"
                                        t-att-data-bs-target="'.o_wevent_offcanvas_cat_%s' % category.name"
                                        aria-expanded="false">
                                        <t t-out="category.name"/>
                                    </button>
                                </h2>
                                <div t-attf-id="o_wevent_offcanvas_cat_#{category.name}" t-attf-class="o_wevent_offcanvas_cat_#{category.name} accordion-collapse collapse">
                                    <div class="accordion-body pt-0">
                                        <ul class="list-group list-group-flush">
                                            <t t-if="category.is_published and category.tag_ids and any(tag.color for tag in category.tag_ids)" t-foreach="category.tag_ids" t-as="tag">
                                                <li t-if="tag.color" class="list-group-item border-0 px-0">
                                                    <a t-att-href="'/event-types?%s' % keep_query('*', tags=str((search_tags - tag).ids if tag in search_tags else (tag | search_tags).ids))"
                                                        class="text-reset" t-att-title="tag.name">
                                                        <div class="form-check">
                                                            <input class="form-check-input pe-none" type="checkbox" t-attf-name="#{tag.color}" t-att-checked="tag in search_tags"/>
                                                            <label class="form-check-label" t-attf-for="#{tag.color}" t-out="tag.name"/>
                                                        </div>
                                                    </a>
                                                </li>
                                            </t>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        </t>
                        <span class="o_wevent_offcanvas_date"/>
                        <span class="o_wevent_offcanvas_country"/>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <template id="event_types_search_box_input" name="Event Types Search Box">
        <t t-call="website.website_search_box_input">
            <t t-set="_classes" t-valuef="o_wevent_event_searchbar_form flex-grow-1 #{_classes}"/>
            <t t-set="search_type" t-valuef="event_types"/>
            <t t-set="action" t-valuef="/event-types"/>
            <t t-set="display_description" t-valuef="true"/>
            <t t-set="display_detail" t-valuef="false"/>
            <t t-set="search" t-value="search or searches and searches['search']"/>
            <t t-set="placeholder">Search an event...</t>
            <t t-foreach="searches" t-as="item">
                <input t-if="item != 'search' and item_value != 'all'" type="hidden"
                    t-att-name="item" t-att-value="item_value"/>
            </t>
            <t t-out="0"/>
        </t>
    </template>

    <template id="searched_tags" name="Searched tags">
        <div class="d-flex align-items-center">
            <t t-foreach="search_tags" t-as="tag">
                <span t-attf-class="d-inline-flex align-items-baseline rounded border ps-2 me-2 mb-2 #{'o_tag_color_%s' % tag.color if tag.color else ''}">
                    <t t-out="tag.display_name"/>
                    <a t-att-href="'/event-types?%s' % keep_query('*', tags=str((search_tags - tag).ids))" class="btn border-0 py-1 text-white">&#215;</a>
                </span>
            </t>
        </div>
    </template>


    <!-- Index - Events list -->
    <template id="events_list" name="Events list">
        <!-- Options -->
        <t t-set="opt_index_sidebar" t-value="is_view_active('event_waitlist_website.opt_index_sidebar')"/>
        <t t-if="opt_events_list_columns" t-set="opt_event_size" t-value="opt_index_sidebar and 'col-md-6' or 'col-md-6 col-lg-4 col-xl-3'"/>
        <t t-else="" t-set="opt_event_size" t-value="opt_index_sidebar and 'col-12' or 'col-xl-12'"/>
        <!-- No events -->
        <!-- Fuzzy search -->
        <div t-if="event_type_ids and original_search" class="col-12 alert alert-warning mt8">
            No results found for '<span t-out="original_search"/>'. Showing results for '<span t-out="searches['search']"/>'.
        </div>
        <!-- List -->
        <div t-foreach="event_type_ids" t-as="event_type" t-attf-class=" #{opt_event_size}">
            <a t-cache="event_type if not editable and event_type.website_published else None" t-attf-href="/event-type/#{ slug(event_type) }" class="text-decoration-none text-reset " t-att-data-publish="event_type.website_published and 'on' or 'off'">
                <article t-attf-class="h-100 #{opt_events_list_cards and 'card'}" itemscope="itemscope" itemtype="http://schema.org/Event">
                    <div t-attf-class="h-100 #{opt_events_list_columns and 'd-flex flex-wrap flex-column' or 'row mx-0'}">
                        <!-- Header -->
                        <header t-attf-class="card-header overflow-hidden bg-secondary p-0 border-0 rounded-0 #{opt_events_list_columns and 'col-12' or 'col-4 col-lg-3'} #{(not opt_events_list_cards) and 'shadow-sm'}">
                            <!-- Image + Link -->
                            <div class="d-block h-100 w-100">
                                <t t-call="website.record_cover">
                                    <t t-set="_record" t-value="event_type"/>
                                    <small t-if="not event_type.website_published" class="o_wevent_unpublished position-absolute bottom-0 px-3 py-2 w-100 text-bg-danger">
                                        <i class="fa fa-ban me-2"/>Unpublished
                                    </small>
                                </t>
                            </div>
                        </header>

                        <!-- Body -->
                        <main t-attf-class="card-body position-relative d-flex flex-column justify-content-between gap-2 #{opt_events_list_columns and 'col-12 py-3' or 'col-8 col-lg-9 px-4'} #{not opt_events_list_cards and opt_events_list_columns and 'bg-transparent px-0'} #{not opt_events_list_cards and not opt_events_list_columns and 'bg-transparent py-0'}">
                            <div>
                                <div class="d-flex flex-wrap gap-1 small">
                                    <t t-foreach="event_type.tag_ids.filtered(lambda tag: tag.category_id.website_id == website or not tag.category_id.website_id)" t-as="tag">
                                        <span t-if="tag.color"
                                            t-attf-class="badge mt-3 mt-sm-0 rounded-pill px-2 p-1 #{'o_tag_color_%s' % tag.color if tag.color else 'text-bg-light'}">
                                            <t t-out="tag.name"/>
                                        </span>
                                    </t>
                                </div>
                                <!-- Title -->
                                <h5 t-attf-class="card-title my-2 #{(not event_type.website_published) and 'text-danger'}">
                                    <span t-field="event_type.name" itemprop="name"/>
                                </h5>
                                <!-- Start Date & Time -->
                                <small class="o_not_editable opacity-75" itemprop="description" t-out="event_type.subtitle">
                                </small>
                            </div>
                            <!-- Location -->
                            <small class="o_not_editable fw-bold" itemprop="location" t-out="event_type.address_id" t-options="{'widget': 'contact', 'fields': ['city'], 'no_marker': 'true'}"/>
                        </main>

                        <!-- Footer -->
                        <footer t-if="opt_events_list_columns and opt_events_list_cards"
                            t-att-class="'small align-self-end w-100 %s %s' % (
                                opt_events_list_cards and 'card-footer' or (not opt_events_list_columns and 'py-2 mt-2') or 'py-2',
                                opt_events_list_cards and 'border-top' or 'px-2',
                            )">
                        </footer>
                    </div>
                </article>
            </a>
        </div>
        <!-- Pager -->
        <div class="d-flex justify-content-center my-3">
            <t t-call="website.pager"/>
        </div>
    </template>

    <template id="opt_events_list_columns" inherit_id="event_waitlist_website.events_list" active="True" name="Layout • Columns"/>

    <template id="opt_events_list_cards" inherit_id="event_waitlist_website.events_list" active="True" name="'Cards' Design"/>


    <!-- Index - Sidebar -->
    <template id="index_sidebar" name="Sidebar">
        <div id="o_wevent_index_sidebar" class="col-lg-4 ms-lg-3 ps-xxl-5 mb-5"/>
    </template>

    <!-- Index - Sidebar - About us -->
    <template id="index_sidebar_about_us" inherit_id="event_waitlist_website.index_sidebar" active="True" name="About us" priority="20">
        <xpath expr="//div[@id='o_wevent_index_sidebar']" position="inside">
            <div class="o_wevent_sidebar_block">
                <h6 class="o_wevent_sidebar_title">About us</h6>
                <p>Use this paragraph to write a short text about your events or company.</p>
            </div>
            <div id="oe_structure_website_event_about_us_1" class="oe_structure"/>
        </xpath>
    </template>

    <!-- Index - Sidebar - Follow us -->
    <template id="index_sidebar_follow_us" inherit_id="event_waitlist_website.index_sidebar" active="False" name="Follow us" priority="30">
        <xpath expr="//div[@id='o_wevent_index_sidebar']" position="inside">
            <div class="o_wevent_sidebar_block">
                <h6 class="o_wevent_sidebar_title">Follow Us</h6>
                <div class="o_wevent_sidebar_social mx-n1">
                    <a t-if="website.social_facebook" t-att-href="website.social_facebook" class="o_wevent_social_link"><i class="fa fa-facebook text-facebook" aria-label="Facebook" title="Facebook"/></a>
                    <a t-if="website.social_twitter" t-att-href="website.social_twitter" class="o_wevent_social_link"><i class="fa fa-twitter text-twitter" aria-label="Twitter" title="Twitter"/></a>
                    <a t-if="website.social_linkedin" t-att-href="website.social_linkedin" class="o_wevent_social_link"><i class="fa fa-linkedin text-linkedin" aria-label="LinkedIn" title="LinkedIn"/></a>
                    <a t-if="website.social_youtube" t-att-href="website.social_youtube" class="o_wevent_social_link"><i class="fa fa-youtube-play text-youtube" aria-label="Youtube" title="Youtube"/></a>
                    <a t-if="website.social_github" t-att-href="website.social_github" class="o_wevent_social_link"><i class="fa fa-github text-github" aria-label="Github" title="Github"/></a>
                    <a t-if="website.social_instagram" t-att-href="website.social_instagram" class="o_wevent_social_link"><i class="fa fa-instagram text-instagram" aria-label="Instagram" title="Instagram"/></a>
                    <a t-if="website.social_tiktok" t-att-href="website.social_tiktok" class="o_wevent_social_link"><i class="fa fa-tiktok text-tiktok" aria-label="TikTok" title="TikTok"/></a>
                </div>
            </div>
            <div id="oe_structure_website_event_follow_us_1" class="oe_structure"/>
        </xpath>
    </template>

    <!-- Index - Sidebar - Photos -->
    <template id="index_sidebar_photos" inherit_id="event_waitlist_website.index_sidebar" active="True" name="Photos" priority="40">
        <xpath expr="//div[@id='o_wevent_index_sidebar']" position="inside">
            <h6 class="o_wevent_sidebar_title">Photos</h6>
            <a href="/event">
                <figure class="o_wevent_sidebar_block o_wevent_sidebar_figure figure">
                    <img class="figure-img img-fluid rounded" src="/website_event/static/src/img/event_past_0.jpg" alt=""/>
                    <figcaption class="figure-caption">A past event</figcaption>
                </figure>
            </a>
            <a href="/event">
                <figure class="o_wevent_sidebar_block o_wevent_sidebar_figure figure">
                    <img class="figure-img img-fluid rounded" src="/website_event/static/src/img/event_training_0.jpg" alt=""/>
                    <figcaption class="figure-caption">Our Trainings</figcaption>
                </figure>
            </a>
        </xpath>
    </template>

    <!-- Index - Sidebar - Quotes -->
    <template id="index_sidebar_quotes" inherit_id="event_waitlist_website.index_sidebar" active="True" name="Quotes" priority="60">
        <xpath expr="//div[@id='o_wevent_index_sidebar']" position="inside">
            <div class="o_wevent_sidebar_block card">
                <div class="card-body">
                    <blockquote class="blockquote mb-0">
                        <p><em>Write here a quote from one of your attendees. It gives confidence in your events.</em></p>
                        <footer class="blockquote-footer text-muted">Author</footer>
                    </blockquote>
                </div>
            </div>
        </xpath>
    </template>

    <!-- Event - Description -->
    <template id="event_type_description_full" name="Event Description" track="1">
        <t t-set="hide_submenu" t-value="True"/>
        <t t-set="hide_register_cta" t-value="True"/>
        <t t-call="website.layout">
            <div id="wrap" t-attf-class="o_wevent_event js_event d-flex flex-column h-100">
                <t t-if="not hide_submenu" t-call="event_waitlist_website.navbar"/>
                <section class="h-100" t-cache="event_type if not editable and event_type.website_published else None">
                    <div class="container overflow-hidden pb-5">
                        <div class="row h-100">
                            <div class="col pe-xxl-5">
                                <t t-call="event_waitlist_website.navbar"/>

                                <div id="o_wevent_event_main_col">
                                    <t t-call="website.record_cover">
                                        <t t-set="_record" t-value="event_type"/>
                                        <t t-set="use_filters" t-value="True"/>
                                        <t t-set="use_size" t-value="False"/>
                                        <t t-set="use_text_align" t-value="True"/>
                                        <div class="container d-flex flex-column flex-grow-1 justify-content-around">
                                            <div class="o_wevent_event_title p-3 my-5">
                                                <h1 t-field="event_type.name" class="o_wevent_event_name" itemprop="name" placeholder="Event Title"/>
                                                <h2 t-field="event_type.subtitle" class="o_wevent_event_subtitle" placeholder="Event Subtitle"/>
                                            </div>
                                        </div>
                                    </t>
                                    <div class="mt-4" t-field="event_type.description" itemprop="description"/>

                                    <table class="table table-striped table-hover">
                                        <thead>
                                            <tr>
                                                <th name="th_event_name" class="text-start">Name</th>
                                                <th name="th_event_tags" class="text-start">Typ</th>
                                                <th name="th_event_date_begin" class="text-end">Date Begin</th>
                                                <th name="th_event_date_end" class="text-end">Date End</th>
                                            </tr>
                                        </thead>

                                        <tbody class="event_tbody">
                                            <t t-foreach="event_type.event_ids.filtered(lambda ev: ev.website_published and ev.date_end >= datetime.datetime.now())" t-as="event">
                                                <tr>
                                                    <td name="td_event_name">
                                                        <a t-attf-href="/event/#{ slug(event) }/#{(not event.menu_id) and 'register'}">
                                                            <span t-esc="event.name"/>
                                                        </a>
                                                    </td>
                                                    <td name="td_event_tags">
                                                        <div class="d-flex flex-wrap gap-1 small">
                                                            <t t-foreach="event.tag_ids.filtered(lambda tag: tag.category_id.name == 'Typ')" t-as="tag">
                                                                <span t-if="tag.color"
                                                                    t-attf-class="badge mt-3 mt-sm-0 rounded-pill px-2 p-1 #{'o_tag_color_%s' % tag.color if tag.color else 'text-bg-light'}">
                                                                    <t t-out="tag.name"/>
                                                                </span>
                                                            </t>
                                                        </div>
                                                    </td>
                                                    <td name="td_event_date_begin" class="text-end">
                                                        <span t-esc="event.date_begin"/>
                                                    </td>
                                                    <td name="td_event_date_end" class="text-end">
                                                        <span t-esc="event.date_end"/>
                                                    </td>
                                                </tr>
                                            </t>
                                        </tbody>
                                    </table>

                                </div>
                            </div>
                            <!-- Sidebar -->
                            <div class="d-lg-flex justify-content-end col-lg-4 col-xl-3 mb-3 mb-lg-0 d-print-none">
                                <div class="mt-3">
                                    <!-- Tickets Desktop -->
                                    <div class="container d-none d-lg-block">
                                        <t t-call="event_waitlist_website.registration_template"/>
                                    </div>

                                    <div class="d-flex flex-wrap gap-1 small">
                                        <t t-foreach="event_type.tag_ids" t-as="tag">
                                            <span t-if="tag.color"
                                                t-attf-class="badge mt-3 mt-sm-0 rounded-pill px-2 p-1 #{'o_tag_color_%s' % tag.color if tag.color else 'text-bg-light'}">
                                                <t t-out="tag.name"/>
                                            </span>
                                        </t>
                                    </div>

                                    <!-- Organizer -->
                                    <div t-if="event_type.organizer_id" class="o_wevent_sidebar_block border-bottom pb-3 mb-3">
                                        <h6 class="o_wevent_sidebar_title">Organizer</h6>
                                        <h4 t-field="event_type.organizer_id" class="font-sans-serif mt-0 mb-1" style="font-size: 1rem"/>
                                        <div class="small" itemprop="location" t-field="event_type.organizer_id" t-options="{'widget': 'contact', 'fields': ['phone', 'mobile', 'email']}"/>
                                    </div>
                                    <!-- Social -->
                                    <div class="o_wevent_sidebar_block">
                                        <h6 class="o_wevent_sidebar_title">Share</h6>
                                        <p>Find out what people see and say about this event, and join the conversation.</p>
                                        <t t-snippet-call="website.s_share">
                                            <t t-set="_no_title" t-value="True"/>
                                            <t t-set="_classes" t-valuef="o_wevent_sidebar_social mx-n1"/>
                                            <t t-set="_link_classes" t-valuef="o_wevent_social_link"/>
                                        </t>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </div>

            <t t-call="event_waitlist_website.modal_waiting_list_ticket_registration"/>
        </t>
    </template>

    <template id="registration_configure_event_type_button" name="Event Template Configuration Button">
        <a t-attf-class="o_not_editable text-nowrap {{linkClasses or '' }}" t-attf-href="/web#id=#{event_type.id}&amp;menu_id=#{backend_menu_id}&amp;view_type=form&amp;model=event.type" role="link"  title="Configure event template">
            <i class="fa fa-gear me-1" role="img" aria-label="Configure" title="Configure event template"/><em>Configure Template</em>
        </a>
    </template>

    <!-- Event - Registration -->
    <template id="registration_template" name="Registration">
        <!-- Show the tickets button only on desktop -->
        <div t-attf-class="d-flex d-lg-block flex-wrap justify-content-between align-items-center {{event_type.open_waiting_list and 'pb-3 border-bottom' or ''}}">
            <div class="d-flex flex-wrap justify-content-between align-items-center w-100 mb-3">
                <h4 class="mb-0">Event Template</h4>
                <t t-call="event_waitlist_website.registration_configure_event_type_button" t-if="request.env.user.has_group('event.group_event_manager')">
                    <t t-set="linkClasses" t-translation="off">ms-2</t>
                </t>
            </div>
            <button t-if="event_type.open_waiting_list" type="button" data-bs-toggle="modal" data-bs-target="#modal_waiting_list_ticket_registration" class="btn btn-primary w-100">Join Waiting List</button>
        </div>
        <div t-if="toast_message" class="o_wevent_register_toaster d-none" t-att-data-message="toast_message"/>
<!--        <div t-if="not event_type.open_waiting_list" class="mb-3">-->
<!--            <div class="alert alert-info mb-0 d-flex flex-wrap gap-2 justify-content-between align-items-center" role="status">-->
<!--                <t t-if="not event_type.event_registrations_started and not event_type.event_registrations_sold_out">-->
<!--                    <div class="d-flex flex-column">-->
<!--                        <em class="me-2">Ticket Sales starting on-->
<!--                            <span class="" t-out="event.start_sale_datetime"-->
<!--                                t-options="{'widget': 'datetime', 'tz_name': event.date_tz, 'format': 'short'}"/>-->
<!--                            <span t-out="event.date_tz"/>-->
<!--                        </em>-->
<!--                        <t t-call="event_type.registration_configure_tickets_button"-->
<!--                            t-if="request.env.user.has_group('event.group_event_manager')">-->
<!--                                <t t-set="linkClasses" t-translation="off">alert-link my-3</t>-->
<!--                        </t>-->
<!--                    </div>-->
<!--                    <button class="btn btn-danger" disabled="1">Registrations not yet open</button>-->
<!--                </t>-->
<!--                <t t-else="">-->
<!--                    <div>-->
<!--                        <em t-if="event_type.event_registrations_sold_out">Tickets for this Event are <b>Sold Out</b></em>-->
<!--                        <em t-else="">Registrations are <b>closed</b></em>-->
<!--                    </div>-->
<!--                    <button class="btn btn-danger py-2 w-100" disabled="1">-->
<!--                        <span t-if="event_type.event_registrations_sold_out">Sold Out</span>-->
<!--                        <span t-else="">Registrations Closed</span>-->
<!--                    </button>-->
<!--                </t>-->
<!--            </div>-->
<!--        </div>-->
    </template>

    <template id="modal_waiting_list_ticket_registration" name="Modal for Waiting List Tickets Registration">
        <!-- Modal -->
        <div class="modal fade" id="modal_waiting_list_ticket_registration" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <div class="o_wevent_registration_title modal-title fs-5" id="staticBackdropLabel">Tickets</div>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>

                    <form
                        id="waiting_list_form"
                        t-attf-action="/event-type/#{slug(event_type)}/waiting-list-registration/new" method="post" class="js_website_submit_form">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>


                        <div class="modal-content">
                            <t t-set="counter" t-value="0"/>
                            <t t-set="input_type_by_question_type" t-value="{'name': 'text', 'email': 'email', 'phone': 'tel', 'company_name': 'text'}"/>
<!--                            <t t-if="availability_check" t-foreach="tickets" t-as="ticket">-->
                            <t t-foreach="range(0, 1)" t-as="att_counter" name="attendee_loop">
                                <t t-set="counter" t-value="counter + 1"/>
                                <div class="modal-body">
                                    <div class="row">
                                        <t t-foreach="event_type.question_ids" t-as="question">
                                            <div class="col-lg-6 mt-2">
                                                <t t-call="event_waitlist_website.waitlist_registration_question">
                                                    <t t-set="registration_index" t-value="counter"/>
                                                </t>
                                            </div>
                                        </t>
                                    </div>
                                </div>
                            </t>

                            <div class="modal-footer border-top">
                                <button type="button" class="btn btn-secondary js_goto_event" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-primary">Confirm Registration</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </template>

    <template id="waitlist_registration_question" name="Registration Event Question">
        <label t-out="question.title"/>
        <span t-if="question.is_mandatory_answer">*</span>
        <t t-if="question.question_type in ['name', 'email', 'phone', 'company_name']">
            <input class="form-control" t-att-type="input_type_by_question_type[question.question_type]"
                   t-attf-name="#{registration_index}-#{question.question_type}-#{question.id}"
                   t-att-required="question.is_mandatory_answer"/>
        </t>
        <t t-elif="question.question_type == 'simple_choice'">
            <select t-attf-name="#{registration_index}-#{question.question_type}-#{question.id}"
                    class="form-select" t-att-required="question.is_mandatory_answer">
                <option value=""/>
                <t t-foreach="question.answer_ids" t-as="answer">
                    <option t-out="answer.name" t-att-value="answer.id"/>
                </t>
            </select>
        </t>
        <t t-else="">
            <textarea t-attf-name="#{registration_index}-#{question.question_type}-#{question.id}"
                      class="col-lg-12 form-control" t-att-required="question.is_mandatory_answer"/>
        </t>
    </template>


    <template id="navbar" name="Event Navbar">
        <div class="container my-3">
            <a href="/event-type">
                <i class="fa fa-chevron-left me-2"/>
                <span>Back to event types</span>
            </a>
        </div>
        <div class="mb-3">
            <div class="container d-flex flex-wrap flex-lg-nowrap justify-content-between align-items-center">
                <t t-set="active_submenu" t-value="event_type.menu_id.child_id.filtered(lambda sm: sm._is_active())"></t>
                <h6 t-field="event_type.name" t-attf-class="flex-grow-1 #{'my-2' if active_submenu else 'mb-0'}"/>
                <div class="d-flex flex-grow-1 align-items-center justify-content-end gap-2">
                    <nav t-if="active_submenu" class="navbar navbar-light navbar-expand-md p-0 d-md-none">
                        <div class="container align-items-baseline">
                            <div id="o_wevent_event_submenu" class="dropdown nav-item">
                                <button class="dropdown-toggle btn btn-light" data-bs-toggle="dropdown">
                                    <t t-out="active_submenu.name if len(active_submenu) == 1 else ''"/>
                                </button>
<!--                                <ul class="dropdown-menu flex-md-wrap w-100" t-att-data-menu_name="editable and 'Event Menu'" t-att-data-content_menu_id="editable and event.menu_id.id">-->
<!--                                    <t t-foreach="event_type.menu_id.child_id" t-as="submenu">-->
<!--                                        <t t-call="website.submenu">-->
<!--                                            <t t-set="link_class" t-value="'dropdown-item'"/>-->
<!--                                        </t>-->
<!--                                    </t>-->
<!--                                </ul>-->
                            </div>
                        </div>
                    </nav>
                </div>
            </div>
            <nav class="navbar navbar-light navbar-expand-md d-none d-md-block p-0">
                <div class="container align-items-baseline">
                    <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#o_wevent_event_submenu" aria-controls="o_wevent_event_submenu" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div id="o_wevent_event_submenu" class="collapse navbar-collapse">
                        <ul class="navbar-nav flex-md-wrap w-100" t-att-data-menu_name="editable and 'Event Menu'" t-att-data-content_menu_id="editable and event_type.menu_id.id">
                            <t t-foreach="event_type.menu_id.child_id" t-as="submenu">
                                <t t-call="website.submenu">
                                    <t t-set="item_class" t-value="'nav-item'"/>
                                    <t t-set="link_class" t-value="'nav-link'"/>
                                </t>
                            </t>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
    </template>


    <template id="registration_waitlist_complete" name="Waiting List Registration Completed">
        <t t-call="website.layout">
            <div class="container my-5 o_wereg_confirmed">
                <div class="row mb-3">
                    <div class="col-12">
                        <h3>Registration confirmed!</h3>
                        <u><a class="h4 text-primary" t-out="event_type.name" t-att-href="event_type.website_url" /></u>
                    </div>
                </div>

                <div class="row mb-3 o_wereg_confirmed_attendees">
                    <div class="col-md-4 col-xs-12 mt-3" t-foreach="waiting_list_ids" t-as="waiting_list_id">
                        <div class="d-flex flex-column">
                            <span t-if="waiting_list_id.name"  class="fw-bold text-truncate">
                                <t t-out="waiting_list_id.name"/>
                            </span>
                            <span t-if="waiting_list_id.email" class="text-truncate">
                                <i class="fa fa-envelope me-2"/>
                                <t t-out="waiting_list_id.email"/>
                            </span>
                            <span t-if="waiting_list_id.phone">
                                <i class="fa fa-phone me-2"/>
                                <t t-out="waiting_list_id.phone"/>
                            </span>
                            <span>
                                <i class="fa fa-ticket me-2"/>
                                <t t-if="waiting_list_id.event_type_id">
                                    <t t-out="waiting_list_id.event_type_id.name"/> (Ref: <t t-out="waiting_list_id.id"/>)
                                </t>
                                <t t-else="">Ref: <t t-out="waiting_list_id.id"/></t>
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

</odoo>
